<div id="activity-form">
  <h3 class="title-edit">Add an Activity</h3>

  <div data-controller="edit-activity">

      <%= simple_form_for @activity, data: { controller: 'flatpickr' } do |f| %>
        <%= f.input :title, input_html: { class: 'special' } %>
        <%= f.input :description, as: :text %>
        <%= f.input :location,
          input_html: {data: {address_autocomplete_target: "address"}, class: "d-none"},
          wrapper_html: {data: {controller: "address-autocomplete", address_autocomplete_api_key_value: ENV["MAPBOX_API_KEY"]}}
        %>
        <%= f.input :time, as: :string, placeholder: "Select your date", input_html: { data: { flatpickr_target: "time" } } %>
        <% if f.object.new_record? %>
          <%= f.input :photos, as: :file, input_html: {multiple: true} %>
        <% end %>
        <%= f.submit class: '' %>
      <% end  %>
  </div>
</div>
